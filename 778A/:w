package main

import (
	"fmt"
	"strings"
)

func main() {
	var t []byte
	var p []byte
	var move int

	fmt.Scan(&t, &p)

	for i := len(t)-1; i >= 0; i-- {
		var a int
		fmt.Scan(&a)
		move++
	}

	fmt.Printf("%d\n", move)
}
